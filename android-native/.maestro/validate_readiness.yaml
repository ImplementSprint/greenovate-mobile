appId: com.pharmacare.pos
---
# Default field values (uat + https://api.example.com) produce "Ready for deployment"
# so we just tap validate and assert the result — no text entry needed.
- launchApp
- tapOn:
    id: "com.pharmacare.pos:id/validateButton"
- assertVisible:
    text: "Ready for deployment"
